# 세금계산서 확인금액

대시보드 공통 기간·거래처 필터 아래에서 등록된 계산서의 공급가액, VAT, 합계금액과 원문 근거를 조회한다. 이 영역은 계산서 **작성일** 기준이다. 기존 발주일 기준 주문 매출, 무료제공 평가금액, 목표 달성률에는 금액을 더하지 않는다.

`public.tax_invoice_amounts`는 별도로 설치된 원장이다. 승인번호가 고유하며 고객 ID로 거래처를 연결한다. 현재는 확인된 일부 자료만 등록되어 있으므로 패널에 항상 일부 자료와 조회 건수·전체 등록 건수를 표시한다. 수정계산서는 부호를 보존한다. 등록 자료 없음은 매출 0원을 의미하지 않는다.

조회는 로그인된 Supabase 클라이언트의 SELECT만 사용한다. 승인번호 순서를 포함한 페이지 조회와 전체 건수 대조를 거쳐 전부 읽은 경우만 금액을 표시한다. 중간 페이지 실패, 건수 변경, 중복 승인번호, 잘못된 날짜·금액은 조회 오류로 처리하고 부분 합계를 표시하지 않는다. 20초 제한이 지나면 오류 상태로 전환한다. 진행 중인 읽기가 나중에 끝나도 로그아웃이나 더 최근 조회 결과를 덮어쓰지 않는다.

여러 페이지인 경우 전체 내용을 다시 조회해 두 결과가 일치할 때만 표시한다. 건수가 같은 삭제·삽입 또는 금액 변경도 검출한다. 이는 연속 조회 간 내용 대조이며 DB 트랜잭션 스냅샷을 보장하지 않는다. 대량 동시 갱신이 필요한 운영으로 바뀌면 RLS를 보존하는 단일 트랜잭션 조회가 필요하다. 로그아웃 때 표시 내용과 진행 중 조회를 무효화하며 새 로그인은 독립적으로 조회한다.

근거는 HTTPS의 Google Sheets와 FLEX 문서 링크만 표시한다. 연결된 서비스의 기존 권한을 그대로 사용하며 공유 권한을 바꾸지 않는다. 원장 값·주문별 배분·실제 출고·입금 정보는 이 화면에서 수정하지 않는다.

배포 시 `tax-ledger.js`를 포함하고 index.html의 app.js, dashboard-ui.js, tax-ledger.js, style.css 버전을 함께 반영한다. 일반 사용자 로그인에서 조회 권한과 근거 링크 접근은 별도로 확인해야 한다. 일반 계정의 새 원장 쓰기 권한은 필요 없다.

검증에는 가상 거래처·금액만 사용한다. 실제 영업 원장과 승인 문서는 공개 저장소나 테스트에 포함하지 않는다.
