# 변경 요청 승인

관리자도 변경 요청을 제출한 뒤 별도 `admin.html` 페이지에서 승인해야 확정 자료가 바뀝니다. 본인이 제출한 요청을 본인이 승인할 수 있습니다. 일반 회사 사용자는 본인의 요청 내역을 확인할 수 있고, 승인·반려는 서버에서 확인된 관리자만 할 수 있습니다.

## 처리 범위

주문·품목, 입금/출고/운송장, 거래처, 제품/제품 상세, 재고 예정, 일정, 서류, 세금계산서 발행 상태, 목표 설정, Excel/RSP 입력 및 Drive 증빙 연결/연결 해제를 요청으로 제출합니다. 삭제도 승인 대상입니다. 대기 중인 신규건은 집계에 포함하지 않고 기존 수정/삭제 건의 확정값은 유지합니다.

위킵의 자동 원본 수집은 별도 조회용 스냅샷으로 유지합니다. DB에서 시트로 보내는 업무 데이터 동기화는 승인 트랜잭션에서만 발생합니다. 읽기 전용 세금계산서 금액 원장은 새 편집 권한을 만들지 않습니다.

기존 인보이스 Storage 직접 첨부 추가·삭제는 승인 절차가 없으므로 비활성화하고 승인 가능한 Drive 증빙 연결을 안내합니다. 서류 파일 업로드는 고유한 경로에 보관한 후 경로 변경을 승인 요청으로 보냅니다. 기존 파일을 덮어쓰지 않습니다. 반려/취소된 임시 파일의 자동 정리는 포함하지 않았습니다.

DB 설치는 `storage.objects`에도 제한 정책을 추가합니다. 회사 사용자라도 documents/invoice-docs 버킷의 기존 파일을 UPDATE/DELETE하지 못하고, 신규 documents 파일은 pending/UUID/파일명 경로만 INSERT할 수 있습니다. 기존 파일 읽기는 유지합니다. 이 제한은 클라이언트에서 Storage API를 직접 호출해도 적용됩니다.

## 검증과 이력

서버가 허용 테이블/필드, 기본키, 값의 형식, 필수 항목, 외래키·고유 제약, 변경 전 전체 행과 주문 품목/삭제되는 증빙 스냅샷을 검사합니다. 제출 검증은 하위 트랜잭션을 롤백하여 원본과 동기화 큐를 유지합니다. 승인 때 같은 검사를 다시 수행하고 전체 요청을 한 트랜잭션으로 반영합니다. 실패하면 대기 상태와 원본을 유지합니다. 반려 사유는 필수입니다.

검증 통과는 입력 구조와 데이터 충돌 검사를 뜻합니다. 계약서·거래명세서 등 원본 증빙과 실제 거래 내용이 맞는지는 관리자가 확인합니다. 수량·단가로 계산하는 금액의 업무적 의미나 올바른 판매 유형을 자동으로 확정하지 않습니다.

관리자 식별은 `auth.users`의 확인된 이메일 `entropyadmin@entropy.internal` 또는 회사 사용자의 서버 관리 `raw_app_meta_data.dashboard_admin=true`입니다. 사용자 편집 메타데이터를 사용하지 않습니다. 관리자 추가는 이 기능의 일반 요청으로 처리하지 않습니다.

요청자·시각·사유·전후 내용·처리자·시각·반려/검토 사유를 비공개 `approval_private.requests`에 보존합니다. 클라이언트는 직접 이 테이블을 수정할 수 없습니다. 요청은 1~1000개 작업, 5MB 이하로 제한합니다. 관리자 페이지 목록/필터/대기 건수는 현재 페이지 기준입니다.

## 운영 설치 순서

현재 파일은 설치 준비본입니다. 배포 상태는 작업 인수인계 또는 실제 원격 상태를 확인합니다.

1. 최신 main과 통합하고 `npm run check`, `npm test`를 실행합니다. GitHub Pages에 새 파일이 배포되기 전 기존 운영 SQL 정의·ACL·트리거를 비밀정보 없이 별도 백업합니다.
2. 기존 확인된 프로젝트에서 `sql/change-approvals.sql`을 하나의 트랜잭션으로 실행합니다. 기존 업무 행은 바꾸지 않지만 이후 직접 쓰기가 차단되므로 프론트 배포와 연속해서 진행합니다. 오래 열린 구버전 화면의 저장은 실패하며 새로고침이 필요합니다.
3. 새 `app.js`, `index.html`, `drive-documents.js`, `change-requests.js`, `admin.html`, `admin-approvals.js`, `approvals.css`를 기존 정적 파일과 함께 배포합니다. 쿼리 버전을 유지합니다. DB 설치 전 프론트가 먼저 열리면 요청 RPC 오류로 실패하며 직접 쓰기로 돌아가지 않습니다.
4. 회사 계정의 기존 확정 자료 읽기, 관리자 페이지 권한 및 빈/기존 요청 목록, 기존 로그인과 위킵 조회를 확인합니다. 검증용 가짜 업무 자료를 운영에 넣지 않습니다. 다음 실제 변경 요청에서 요청 ID와 미반영 상태를 확인하고, 관리자가 내용을 확인한 뒤 승인/반려합니다.
5. 원격 배포 해시 및 공개 파일 일치, DB RPC 존재/권한/트리거와 보안 advisor를 확인하고 인수인계에 기록합니다.

## 복구

요청 처리 중 장애라면 재시도 전 요청 상태를 조회합니다. 승인 완료를 다시 적용하지 않습니다. 이미 승인된 업무 변경을 배포 복구로 자동 되돌리지 않습니다.

구버전 프론트로 복구만 하면 직접 저장 차단이 유지됩니다. 기존 직접 저장 체계로 돌아가려면 운영자의 명시적 결정 후 12개 보호 테이블의 `require_change_approval`, `require_approval_truncate` 트리거를 제거하고 새 RPC 실행 권한을 회수해야 합니다. 요청 이력 테이블은 삭제하지 않습니다. 이전에 부여돼 있던 TRUNCATE 권한은 위험하고 일반 UI에 필요하지 않으므로 복구 과정에서도 다시 부여하지 않습니다.

기존 파일 편집 기능까지 되돌리기로 결정한 경우에만 storage.objects의 approval_storage_insert/update/delete 제한 정책을 제거합니다. 기본 회사 접근 정책이나 파일 원본은 삭제하지 않습니다.

DB 소유자·서비스 관리자의 SQL 권한 자체를 제한하는 기능은 아닙니다. 소유자는 스키마를 변경할 수 있으므로 별도 운영 접근 관리가 필요합니다.

## 검증 근거

합성 자료의 독립 PGlite DB에서 승인 전 보존·자기 승인·다른 사용자 승인 거부·직접 쓰기/일괄 비우기 차단·충돌·중복/재전송·반려·원자적 롤백·삭제 연쇄를 검사합니다. 기존 폼 테스트는 즉시 저장 성공 대신 요청 제출 후 확정 상태 보존을 확인합니다. 관리자 페이지는 XSS 이스케이프, 전후 비교, 삭제 자식 자료, 로그인 전환/응답 순서, 오류 상태, 명시적 승인/반려를 검사합니다.
