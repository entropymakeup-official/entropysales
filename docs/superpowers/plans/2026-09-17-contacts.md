# Contacts Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development for bounded tasks with separate file ownership and review.

**Goal:** 거래처 연락처 메뉴에 직접 입력 및 엑셀/CSV 검토·선택 업로드 제공.
**Architecture:** 독립 UMD 모델/UI 및 importer, 기존 ChangeRequests 승인 클라이언트, additive contacts 테이블.
**Tech Stack:** Vanilla JS, existing SheetJS, Supabase/Postgres, node:test/PGlite.
**Spec:** ../specs/2026-09-17-contacts.md

## Global Constraints
- 기존 회사 로그인·승인 절차 유지. 실제 연락처 공개 저장소 금지.
- 15MB/5,000행 입력, 요청 1,000 operations/5MB 이하.
- 승인대기를 저장완료로 표시하지 않는다. 확정값 덮어쓰기 없음.
- 다른 작업의 worktree 변경 금지.

## Task 1: Database and approval integration
- [ ] Add tests/contacts-db.test.cjs based on existing contracts-db fixture; assert `submit({table:'contacts',action:'insert',values:{company:'Sample',person:'Contact',email:'a@example.test'}})` returns pending and SELECT remains empty until admin review.
- [ ] Run `node --test tests/contacts-db.test.cjs`, observe missing schema failure.
- [ ] Add sql/contacts.sql: columns from spec, company identity SELECT, require_approval triggers, validate length/status/email/contact, case-insensitive duplicate indexes; patch installed allowlist via guarded replacement preserving all other code.
- [ ] Test anonymous/outside-company/direct-write denial, stale updates, duplicate email/name, batch rollback and existing pending preservation.
- [ ] Add contacts field labels and readable batch summary in admin-approvals.js; validate tests.

## Task 2: Model, controller and contacts UI
- [ ] Add tests/contacts.test.cjs using `assert.throws(()=>Contacts.normalize({company:'A',person:'B'}),/연락/)`; controller pending save must not append approved rows, retry must not resubmit successful batches, account reset must suppress stale async results.
- [ ] Verify RED then implement contacts.js and contacts.css. Mount contract: `Contacts.mount({document,sb,client,getCustomers,onMessage})` returns show/hide/controller/rows/reload and importer hook. Pure normalize, duplicate keys, filter/render helpers exported.
- [ ] Controller provides saveMany(rows,{reason,onProgress}) returning {done,failed,cancelled}; done means request accepted with pending status and failed retains row/error. Paginate approved and pending reads, refresh pending before submissions, cap batches by count and byte size.
- [ ] Direct form linked customer autofill, explicit status fields and optional detail groups; busy/epoch guards, search/filter/list and empty/loading/error states.

## Task 3: Import and app wiring (root)
- [ ] Add tests/contacts-import.test.cjs for BOM/header aliases, split names, malformed email, cross-file/existing duplicate rows, phones as strings and safe escaped review HTML; RED before implementation.
- [ ] Implement contacts-import.js using supplied attachment as reference; export parseTable/classify/renderReview/mount. Workbook read through existing XLSX, encoding and sheet choices, checkbox review and template download; selected save uses controller.saveMany and preserves failures.
- [ ] Wire index.html scripts/styles/menu, app.js go/title/show/hide/bootstrap, package check scripts, admin asset version.
- [ ] Run full `npm test` and `npm run check`, synthetic browser QA, independent code review, fixes/retest.
- [ ] Document usage and verified state; prepare commit/PR and additive DB rollout, verify deployed assets and empty protected table. Do not seed actual or synthetic contacts into production.

## Execution ledger
- User approved implementation including direct/Excel/CSV 2026-09-17.
- Isolated worktree dashboard-contacts-20260917 from origin/main 0e2881c.
- Ruling: execute authorized implementation without another design/plan confirmation; concrete design already accepted.

- Task 1 complete: schema/admin targeted19 PASS, independent review clean.
- Task 2 complete: model/UI13 PASS, partial pending patch regression fixed and re-reviewed.
- Task 3 local implementation complete: importer8 PASS; browser direct save, CSV/XLSX, sheet switch, leading zeros, duplicate/invalid review, pending submission, partial failure selection retention and auth cancellation PASS. Final all333 tests/checks PASS pending command output confirmation. Deployment pending.
- Ruling: pending duplicate preview uses only requests accessible under existing approval policy; preserve current authorization. DB uniqueness blocks cross-user duplicates at approval.
