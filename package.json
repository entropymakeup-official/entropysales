{
  "name": "entropysales-dashboard",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check app.js && node --check dashboard-model.js && node --check dashboard-ui.js"
  }
}
