{
  "name": "entropysales-dashboard",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check app.js && node --check invoice-amounts.js && node --check dashboard-model.js && node --check dashboard-ui.js && node --check sheet-sync-status.js && node --check live-inventory.js && node --check inventory-history.js && node --check inventory-monthly.js && node --check drive-documents.js && node --check change-requests.js && node --check admin-approvals.js"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8"
  }
}
