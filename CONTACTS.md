# 거래처 연락처

거래처 마스터 아래 **거래처 연락처** 메뉴에서 업체별 담당자를 관리합니다.

- **직접 입력:** 업체명, 담당자, 연락수단 하나 이상과 등록 사유를 입력합니다. 기존 거래처 선택 시 업체·국가가 채워집니다. 거래처 연결 없이도 등록할 수 있습니다.
- **엑셀·CSV 업로드:** 파일을 선택하고 필요한 시트를 고릅니다. CSV 한글이 깨지면 인코딩을 바꿉니다. Google·Outlook의 일반적인 내보내기 머리글도 인식합니다.
- **빈 양식:** CSV 머리글 양식을 다운로드합니다. 전화번호는 엑셀에서 텍스트로 입력해 앞자리 0과 국가번호를 보존하세요. 원본에서 이미 사라진 0은 복원하지 않습니다.
- 미리보기에서 업체명·담당자·연락수단 누락, 잘못된 이메일, 기존/파일 내 중복을 확인합니다. 정상 행 중 체크한 항목만 등록 요청합니다. 기존 연락처는 덮어쓰지 않습니다.
- 등록·수정은 **관리자 승인 요청**입니다. 접수된 항목은 승인 대기로 표시되며, 승인 후 새로고침하면 확정 목록에 표시됩니다. 일반 사용자는 본인의 대기 요청만 조회할 수 있고 관리자는 전체를 볼 수 있습니다. 다른 사용자의 대기 요청과 겹치는 연락처는 DB의 승인 시 중복 검사로 차단됩니다.
- 파일 상한 15MB/5,000행. 내부 요청은 1,000건/5MB 이하로 분할합니다. 부분 실패 시 접수된 항목을 제외한 선택 항목만 재시도합니다. 잘못된 행은 원본 파일에서 수정 후 다시 업로드하세요.

## 운영 적용

기존 승인 기능 및 contracts 설치 후 `sql/contacts.sql`을 한번 적용합니다. 새 연락처 테이블과 회사 계정 읽기 정책을 추가하고 기존 승인 함수의 대상 목록만 확장합니다. 기존 영업 데이터와 대기 요청은 수정하지 않습니다. DB 테이블의 직접 쓰기는 금지되며 승인 RPC를 사용합니다. 새 함수를 포함한 권한 검증은 `tests/contacts-db.test.cjs`에서 PGlite로 수행합니다.

프런트엔드는 contacts.js/css, contacts-import.js/css와 index/app, 관리자 필드 표시를 함께 배포합니다. 실제 연락처 파일·미리보기 데이터는 저장소에 넣지 않습니다. 파일 원본은 서버에 업로드하지 않으며 브라우저에서 읽어 선택한 연락처만 기존 승인 API로 전송합니다.

검증: `npm test`, `npm run check`; 로컬 합성자료 브라우저에서 직접입력, XLSX 시트선택, CSV 검토, 중복·오류 제외, 부분실패 선택 유지, 계정전환 폐기를 확인합니다.
