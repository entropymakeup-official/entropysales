# 계약서 관리

거래처 마스터 바로 아래 계약서 메뉴에서 거래처별 복수 계약을 관리합니다. 거래처 목록의 계약서 링크는 해당 거래처로 필터링합니다.

- 기본정보, 계약기간/갱신거절 통보기한, 계약조건/판매범위, 수금조건, FOC, 담당자/특약을 등록합니다.
- 수금조건은 선금/잔금 비율, 각각의 지급 기준·기한, 통화, 결제수단, 수수료 부담 및 특약입니다. 실제 입금 상태를 변경하지 않습니다.
- 종료일 당일까지 유효합니다. 유효 상태의 계약은 종료일 다음날부터 화면에서 만료로 표시합니다. 자동갱신은 조건 정보이며 종료일을 자동 연장하지 않습니다.
- 유효 계약의 만료일·통보기한이 90일 이내이면 목록에 표시합니다. 30일 이내는 강조하며 통보기한 경과도 구분합니다. 메일/푸시 알림은 보내지 않습니다.
- 원본·변경합의서는 기존 서류 관리에 등록·승인된 해당 거래처의 계약서를 연결합니다. 파일 복사/삭제를 하지 않습니다. 한 계약에 서명본과 부속합의서 각 1개를 연결합니다.
- 갱신 계약은 새로 등록하여 이전 계약을 연결합니다. 자기 참조/순환 및 다른 거래처의 이전 계약 연결은 차단합니다. 기존 자료 변경 이력은 변경 요청·관리자 승인 화면에서 조회합니다.
- 등록·수정은 기존 관리자 승인 절차를 거칩니다. 오류/승인 대기 중 기존 확정값을 유지합니다. 삭제 버튼은 제공하지 않으며 계약 상태를 만료/해지로 관리합니다.

## 설치 및 검증

`sql/change-approvals.sql` 설치가 전제입니다. `sql/contracts.sql`을 한 번 적용한 뒤 프론트를 배포합니다. 기존 승인 함수 정의에서 허용 테이블 목록만 확장하며 예상 정의와 다르면 전체 트랜잭션을 실패시킵니다. contracts와 documents에 추가한 검증 트리거 외 기존 업무 데이터는 바꾸지 않습니다. 기존 승인 SQL 재설치 시 contracts 확장이 제거되므로 별도 검토가 필요합니다.

contracts는 RLS와 회사 로그인 확인을 통해 읽습니다. 브라우저 역할은 SELECT만 허용하고 쓰기는 기존 승인 함수가 수행합니다. 연결된 거래처/서류/이전 계약 삭제는 FK RESTRICT로 차단합니다. 신규·변경된 서류 연결은 거래처/종류를 확인하며 기존 ID 연결은 거래처명 변경 후에도 보존합니다.

검증: `npm run check`, `npm test`. 합성자료 PGlite에서 승인 전 보존, 관리자만 승인, 외부사용자 차단, 직접쓰기 차단, 수금/기간/FOC 제약, 이전 계약 및 서류 연결, 동시 편집 충돌, 반려, 거래처명 변경을 검증합니다. 브라우저 합성자료로 목록/상세/수금 비율 검증/실패 시 입력 유지/등록 요청/모바일 폼을 확인했습니다.

복구는 프론트를 이전 배포로 되돌리고 새 테이블·요청 이력을 보존하는 방식이 기본입니다. 실제 계약 등록 후 테이블 삭제나 이력 삭제를 하지 마세요. 승인 함수는 설치 전 백업과 이후 다른 변경을 대조해야 하며 전체 구버전 SQL로 덮어쓰지 않습니다.
