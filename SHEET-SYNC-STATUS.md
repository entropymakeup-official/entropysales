# Google 시트 반영 상태

대시보드·RAW 데이터·인보이스 화면 상단에서 전체 주문의 시트 반영 상태를 확인한다. 대시보드 기간/거래처 필터와는 독립적이다.

- 화면이 열려 있을 때 1분마다 조회한다. ‘상태 다시 확인’은 읽기 전용 조회이며 주문 재전송 버튼이 아니다.
- 기존 Apps Script는 5분마다 최대20주문을 반영한다. 많은 변경은 여러 주기가 필요하다.
- 대기 건수는 오류 건수를 제외한 미반영 주문 변경 건수다. 삭제 반영도 포함된다.
- 오류는 다음 실행에서 재시도하며,15분 이상 미반영은 지연으로 표시한다. 연결 기록이 없는 주문과 DB측 실행 중지도 별도 표시한다.
- 오류/지연/연결 누락 상세는 최대10건을 표시한다. 더 많으면 전체 문제 건수를 함께 표시한다.
- 마지막 반영 시각은 시트 처리 후 DB에 완료가 기록된 시간이다. 새 변경이 없으면 오래된 시각이어도 정상이다. 이 값이 Apps Script의 최근 예약 실행 성공을 보장하지는 않는다.
- 조회 실패나20초 초과 응답은 ‘상태 확인 불가’로 표시하고 다음 조회에서 복구한다. 이전 성공 결과를 현재 완료로 표시하지 않는다. 로그아웃/관련 없는 화면으로 이동하면 조회와 화면 표시를 중단한다.

## 설치와 검증

1. 기존 invoice_sheet_private.queue/config 설치 여부와 invoices RLS의 접근 대상을 확인한다.
2. sql/invoice-sheet-status.sql을 적용한다. 이 파일은 get_invoice_sheet_status 읽기 전용 RPC만 추가한다. 원본 주문/시트/기존worker키/5분트리거를 수정하지 않는다.
3. 기존 회사 계정과 내부 관리자 계정만 실행할 수 있고, 익명/다른 도메인/사용자 식별 없는 호출이 거부되는지 검증한다. 직접 private 테이블 권한은 주지 않는다. 기존 invoices RLS의 접근 대상을 향후 변경하면 이 RPC의 동일한 대상 검사도 함께 검토한다.
4. npm ci, npm run check, npm test. UI 결과를 정상/대기/오류/연결실패 및 좁은 화면에서 확인한다.
5. 검토된 PR을 main에 통합하고 Pages 성공, 공개 파일 일치, 로그인한 운영 화면의 실제 상태 조회를 확인한다.

RPC는 집계 건수/시각/문제 주문번호·거래처·정해진 오류 구분만 반환한다. worker secret/hash,lease token,원문 오류 메시지,원본 주문 금액은 반환하지 않는다. 주문 재시도/수정/삭제 기능은 제공하지 않는다.

코드 배포를 되돌릴 때는 이 기능의 프런트 변경만 되돌릴 수 있다. 읽기 RPC가 남아 있어도 기존동기화는 유지된다. 원복 시 기존 queue/트리거/키를 재설치하거나 초기화하지 않는다.
