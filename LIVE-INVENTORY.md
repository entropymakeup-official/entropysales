# 위킵 실시간 재고 화면

`실시간 재고`는 위킵 표에서 수집한 전체 행 스냅샷을 별도로 보여준다. 기존 제품·재고 포캐스팅 자료와 연결하거나 병합하지 않는다.

## 수집 payload 만들기

브라우저에서 수집한 데이터 행을 11열 배열 `cells`로 준비한 뒤 아래 함수를 사용한다. 헤더 행은 제외한다. `expectedCount`는 화면에서 확인한 전체 데이터 행 수여야 한다.

```js
const payload = LiveInventory.fromCells(cells, new Date().toISOString(), expectedCount);
JSON.stringify(payload);
```

열 매핑은 제품명 0, 관리코드 1, 공급처 2, 가용 6, 안전 7, 유보 8, 하자 9이다. 관리코드가 같거나 비어 있어도 행을 제거하지 않는다. 빈 수량, 비정수 수량, 건수가 다른 자료, 24시간보다 오래되거나 5분보다 미래인 자료는 거절한다.

## 화면에서 반영하기

로그인한 대시보드에서 `실시간 재고` → `수집 자료 반영`을 펼치고 payload JSON을 붙여 넣은 뒤 `검증 후 반영`을 누른다. 화면은 `save_wekeep_inventory({p_snapshot})`의 수집 시각과 행 수 확인 응답이 payload와 일치할 때만 저장 완료를 표시한다.

화면을 열어 둔 동안 저장된 자료를 60초마다 다시 확인한다. 다른 페이지로 이동하거나 로그아웃하면 조회 타이머와 화면 상태를 정리한다. 마지막 수집 후 15분이 지나면 지연 상태로 표시하며, 서버 확인 시각을 마지막 수집 시각으로 대신 표시하지 않는다.
